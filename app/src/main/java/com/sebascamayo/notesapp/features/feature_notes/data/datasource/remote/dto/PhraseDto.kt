package com.sebascamayo.notesapp.features.feature_notes.data.datasource.remote.dto

// El dto es la clase en donde se almacena la data remota

class PhraseDto (
    val author: String,
    val phrase: String
    ) {

    /*fun toPhraseEntity(): PhraseEntity {
        return PhraseEntity(
            author = author,
            phrase = phrase
        )
    }*/
}